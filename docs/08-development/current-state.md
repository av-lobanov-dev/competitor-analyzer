# Текущее состояние проекта

Дата актуальности: 2026-07-22

## Проект

Название проекта:

```text
Competitor Analyzer
```

Папка проекта на сервере:

```text
/opt/competitor-analyzer
```

Основная ветка Git:

```text
main
```

## Подтверждённые компоненты

В проекте используются:

- Ubuntu Server;
- Docker;
- Docker Compose;
- PostgreSQL;
- n8n;
- Playwright;
- Node.js;
- GPT;
- Git;
- GitHub.

## Реализованная инфраструктура

- проект размещён на сервере;
- сервисы запускаются через Docker Compose;
- PostgreSQL работает в отдельном контейнере;
- n8n работает в отдельном контейнере;
- Playwright работает в отдельном контейнере;
- Node.js worker подключаются к PostgreSQL;
- репозиторий связан с GitHub;
- изменения отправляются в ветку `main`.

## Исследование сайтов

Реализовано:

- создание заданий исследования сайта;
- получение заданий worker;
- открытие сайта через Playwright;
- переход по страницам;
- сохранение результатов исследования;
- сохранение заголовка страницы;
- сохранение итогового URL;
- сохранение текста страницы;
- сохранение HTML страницы;
- сохранение ссылок;
- сохранение структуры страницы.

## Создание правил парсинга

Реализовано или подготовлено:

- задания с типом `scraping_rule`;
- поле версии промпта;
- получение снимка страницы для анализа;
- PostgreSQL-функция `take_next_scraping_rule_job()`;
- безопасное получение задания;
- использование `FOR UPDATE SKIP LOCKED`;
- перевод задания из `new` в `running`;
- установка времени начала обработки;
- очистка предыдущей ошибки.

## Сбор товаров

Подтверждено:

- существует очередь заданий сбора товаров;
- создан Product Worker;
- worker подключается к PostgreSQL;
- worker получает задания;
- используются правила сбора товаров;
- предусмотрен обход нескольких страниц;
- сбор товаров находится в активной разработке.

Необходимо дополнительно проверить:

- извлечение всех полей товара;
- обработку пагинации на разных сайтах;
- сохранение характеристик;
- сохранение изображений;
- обработку отсутствующих полей;
- повторные попытки;
- обработку частично успешных заданий.

## История цен

Подтверждено:

- история цен связана с заданиями сканирования товаров.

Необходимо проверить:

- правила создания новой записи цены;
- определение изменения цены;
- работу с товарами без цены;
- фиксацию наличия;
- исключение ложных изменений.

## Последние подтверждённые коммиты

```text
9c9a1c9 Add scraping rule job claim function
1a5083d Link price history to product scan jobs
d0ba256 Update project PostgreSQL structure
b8c9bd0 Run Playwright queue workers automatically
aa530f6 Add product scraping rules and job queue
```

## Analysis Worker и SEO-анализ — 2026-07-22

Подтверждена новая архитектура обработки аналитических заданий.

Текущая конфигурация:

```text
ENABLE_ANALYSIS_WORKER=true
ENABLE_SCRAPING_WORKER=false
ENABLE_LEGACY_WORKER=false
```

Реализовано и проверено:

- корневое Node.js-приложение работает внутри контейнера Playwright;
- Analysis Worker получает задания из таблицы `analysis_jobs`;
- маршрутизатор поддерживает типы `site_structure` и `seo_analysis`;
- вместо временного placeholder реализован рабочий SEO-анализатор;
- SEO-анализатор формирует `score`, `grade`, `metrics`, `problems` и `recommendations`;
- тестовое задание `analysis_jobs.id = 12` завершилось со статусом `completed`;
- подтверждён результат `score = 100` и `grade = good`;
- подтверждены 94 ссылки, 20 изображений, 21 заголовок, 40 товарных элементов и 2029 символов текста;
- реализовано восстановление зависших аналитических заданий;
- полный объект задания исключён из журнала получения задания;
- в журнале остаются идентификаторы, тип анализа, заголовок страницы и итоговый URL;
- поля `page_text`, `page_structure` и `possibleProductCards` не попадают в запись получения задания.

Основные файлы:

```text
src/workers/base-worker.js
src/workers/analysis-worker.js
src/services/analysis-service.js
src/services/analysis-router.js
src/queue/analysis-queue.js
src/analyzers/seo-analyzer.js
```

Подтверждённая цепочка обработки:

```text
analysis_jobs
    -> Analysis Worker
    -> Analysis Service
    -> Analysis Router
    -> SEO Analyzer
    -> analysis_jobs.result_json
```

## Текущий этап разработки

Текущий этап состоит из двух параллельных задач:

1. создание полной технической энциклопедии;
2. завершение автоматического сбора товаров.

## Требует инвентаризации

Необходимо получить с сервера фактические данные о следующих частях проекта:

- структура каталогов и файлов;
- список Docker-контейнеров;
- конфигурация Docker Compose;
- список таблиц PostgreSQL;
- структура каждой таблицы;
- список функций PostgreSQL;
- список индексов;
- связи между таблицами;
- список worker;
- механизм автоматического запуска worker;
- список workflow n8n;
- переменные окружения;
- резервное копирование;
- восстановление после сбоя;
- логирование;
- мониторинг.

## Комплексный healthcheck инфраструктуры

Добавлен скрипт:

```text
scripts/healthcheck.sh
```

Проверяет:

- Docker;
- PostgreSQL;
- n8n;
- Playwright;
- память;
- диск;
- UFW;
- Fail2ban;
- резервные копии PostgreSQL;
- Git.

Последняя подтверждённая проверка завершилась успешно:

```text
Ошибок: 0
Предупреждений: 0
Общее состояние: OK
```

## Безопасный шаблон переменных окружения

20 июля 2026 года завершён дополнительный аудит секретов проекта.

Подтверждено:

- файл `.env` не отслеживается Git;
- файл `.env` имеет права доступа `600`;
- резервные пароли в Playwright worker-файлах отсутствуют;
- `POSTGRES_PASSWORD` является обязательной переменной;
- при отсутствии `POSTGRES_PASSWORD` worker завершает работу с понятной ошибкой;
- явные пароли и токены в `docker-compose.yml` отсутствуют;
- устаревший файл `.env.save` удалён;
- создан безопасный файл `.env.example` без настоящих секретов.

Старые упоминания тестового пароля обнаружены в истории Git. История репозитория пока не переписывалась, поскольку действующий пароль PostgreSQL уже был изменён.

## Аудит доступа к n8n и ротация PostgreSQL — 2026-07-20

- n8n использует встроенную пользовательскую аутентификацию по email.
- Первоначальная учётная запись владельца n8n уже настроена.
- Порт n8n привязан только к `127.0.0.1:5678`.
- Порт 5678 отсутствует в разрешающих правилах UFW.
- Reverse proxy и HTTPS пока не настроены.
- `N8N_SECURE_COOKIE=false` используется временно до перехода на HTTPS.
- Устаревшие переменные Basic Auth удалены из `.env`.
- Пароль PostgreSQL повторно ротирован после появления предыдущего значения в диагностическом выводе.
- Проверено, что новый пароль работает, а предыдущий пароль отклоняется.
- В `.env` исправлено имя базы с `competitor_db` на фактическое `competitor_analyzer`.
- Защищённая временная копия `.env` со старым паролем удалена.
- Аудит n8n выявил SQL-ноды без Query Parameters, неиспользуемые credentials и Code nodes, требующие отдельной проверки.

## Проверка действующего секрета в Git — 2026-07-20

- Проверено, что текущий пароль PostgreSQL отсутствует в рабочих файлах репозитория.
- Проверено, что текущий пароль отсутствует в `HEAD`.
- Проверена вся Git-история, включая branches, tags и remote refs.
- Проверены commit patches, reflog и локальные недостижимые Git-объекты.
- Текущий действующий пароль PostgreSQL нигде не обнаружен.
- Файл `.env` игнорируется Git и не отслеживается.
- Старые тестовые пароли могут оставаться в истории Git, но они больше не действуют.

## Ограничение ресурсов Docker — 2026-07-21

- Обнаружены два глобальных OOM-события от 2026-07-15.
- Убитые процессы находились в старом Docker-контейнере и соответствовали Node.js-процессам n8n.
- Для PostgreSQL установлен лимит памяти 256 МБ.
- Для n8n установлен лимит памяти 768 МБ.
- Для Node.js-процесса n8n установлен `--max-old-space-size=512`.
- Для Playwright установлен лимит памяти 768 МБ.
- Для всех сервисов включена ротация Docker-логов: 3 файла максимум по 10 МБ.
- Политика перезапуска `unless-stopped` сохранена.
- Выполнена безопасная очистка остановленных контейнеров, dangling images и Docker build cache.
- Docker volumes и данные PostgreSQL не удалялись.
- После пересоздания контейнеров healthcheck завершился успешно.
- При тяжёлых запусках Playwright рекомендуется закрывать неиспользуемое подключение VS Code Remote, поскольку VS Code Server может использовать более 500 МБ RAM.

## Автоматические резервные копии — 2026-07-21

- Добавлен скрипт `scripts/postgres-backup.sh`.
- Резервная копия PostgreSQL создаётся ежедневно в 02:30 UTC.
- Ежедневные резервные копии хранятся 7 дней.
- По воскресеньям создаётся еженедельная резервная копия.
- Еженедельные резервные копии хранятся 4 недели.
- Для предотвращения одновременных запусков используется `flock`.
- Каждая резервная копия проверяется через `pg_restore --list`.
- Расписание находится в `/etc/cron.d/competitor-analyzer-backup`.
- Healthcheck ищет только файлы `competitor_analyzer*.dump`.
- Файлы журналов больше не принимаются healthcheck за резервные копии.

## Закреплённые версии контейнеров — 2026-07-21

- n8n закреплён на версии `2.29.10`.
- PostgreSQL закреплён на версии `16.14`.
- Для n8n больше не используется плавающий тег `latest`.
- Playwright продолжает собираться из локального Dockerfile.
- Контейнеры пересозданы с закреплёнными образами.
- Версии работающих контейнеров подтверждены.
- Общий healthcheck проекта завершился успешно.

## Поведение healthcheck — 2026-07-21

- Каталог резервных копий определяется абсолютным путём.
- Проверяются файлы `competitor_analyzer*.dump` во всех подкаталогах.
- Успешная проверка возвращает код завершения `0`.
- Healthcheck возвращает ненулевой код при ошибках.
- Состояние `FAILED` больше не может быть ошибочно принято за успешное.
